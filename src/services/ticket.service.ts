//const client = require('../database/conection')

const getTickets = async () => {
    try {
       
    } catch (error) {
        throw error
    }
}

const postTicket = async (user: {name:string, age:number}) => {
    try {
        
       
    } catch (error) {
        throw error
    }
}

const updateTicket = async (user:{ name:string, age:number, id:number}) => {
    try {
        
    } catch (error) {
        throw error
    }
}

const deleteTicket = async (id:number) => {
    try {
        
    } catch (error) {
        throw error
    }
}

export = {
    getTickets, 
    postTicket, 
    updateTicket, 
    deleteTicket,
}